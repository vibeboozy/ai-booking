/**
 * ANCHOR: profile
 * PURPOSE: Route /profile — redirect или hub личного кабинета.
 * Dependencies: @/lib/auth.
 * CRITICAL: Auth required.
 */

export default function ProfilePage() {
  return (
    <main>
      <h1>Профиль</h1>
      {/* TODO: tabs → trips, favorites */}
    </main>
  );
}

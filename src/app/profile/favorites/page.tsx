/**
 * ANCHOR: profile
 * PURPOSE: Route /profile/favorites — сохранённые места.
 * Dependencies: FavoritesList, @/lib/auth.
 * CRITICAL: Auth required.
 */

export default function ProfileFavoritesPage() {
  return (
    <main>
      <h1>Избранное</h1>
      {/* TODO: <FavoritesList /> */}
    </main>
  );
}
